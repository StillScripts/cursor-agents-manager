{
  "name": "agents-manager-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "dev:web": "bun run --filter=web dev",
    "dev:backend": "bun run --filter=backend dev",
    "build": "bun run --filter=web build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "bun run --filter=validators test && bun run --filter=helpers test && bun run --filter=backend test",
    "test:watch": "bun run --filter=validators test:watch & bun run --filter=helpers test:watch & bun run --filter=backend test:watch"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1"
  }
}
